--- ./proc/module.mk	2016-06-08 22:28:44.763085018 -0300
+++ ./proc/module.mk	2016-06-08 22:29:08.224223842 -0300
@@ -42,7 +42,7 @@

 ifeq ($(SHARED),1)
 ALL        += proc/$(SONAME)
-INSTALL    += ldconfig
+#INSTALL    += ldconfig
 LIBFLAGS   := -DSHARED=1 $(FPIC)
 # This is in gcc 3.5, but exported functions must be marked.
 #LIBFLAGS += $(call check_gcc,-fvisibility=hidden,)
